[package]
name = "bittensor-core-py"
version = "0.1.0"
edition = "2021"
publish = false
description = "PyO3 bindings for bittensor-core (PyPI package: bittensor-core, module: bittensor_core)"
license = "Apache-2.0"

[lib]
name = "bittensor_core"
crate-type = ["cdylib"]

[dependencies]
bittensor-core = { path = "../bittensor-core" }
codec = { package = "parity-scale-codec", version = "3.7.5", default-features = false }
hex = "0.4.3"
pyo3 = { version = "0.29", features = ["abi3-py310"] }
scale-info = { version = "2.11.6", default-features = false }

[features]
default = ["ledger"]
# Ledger generic-app support (HID). On in every published wheel: the Linux
# build uses hidapi's pure-Rust hidraw backend (no libudev, no C), so it is
# manylinux-safe. Disable to build a wheel without HID support entirely.
ledger = ["bittensor-core/ledger"]

[lints]
workspace = true
